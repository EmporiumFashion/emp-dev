<header class="site-header">
  <div class="header-inner">

    <!-- Left spacer -->
    <div class="header-left"></div>

    <!-- Logo -->
    <div class="header-logo">
      <a href="/" aria-label="Home">E</a>
    </div>

    <!-- Right actions -->
    <div class="header-actions">
      <a href="/collections/all" class="header-link">Shop</a>
      <a href="/cart" class="header-link">Cart</a>
    </div>

  </div>
</header>

<style>
@import url('https://fonts.googleapis.com/css2?family=UnifrakturMaguntia&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500&display=swap');

/* RESET */
.site-header,
.site-header * {
  box-sizing: border-box;
}

/* HEADER WRAP */
.site-header {
  width: 100vw;
  margin-left: calc(50% - 50vw);
  background: #000;
  border-bottom: 1px solid rgba(255,255,255,0.12);
}

/* INNER GRID */
.header-inner {
  height: 72px;
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  align-items: center;
  padding: 0 28px;
}

/* LOGO */
.header-logo {
  justify-self: center;
}

.header-logo a {
  font-family: 'UnifrakturMaguntia', serif;
  font-size: 2.4rem;
  letter-spacing: 2px;
  text-decoration: none;
  color: #f5f5f2;
}

/* ACTIONS */
.header-actions {
  justify-self: end;
  display: flex;
  gap: 28px;
}

.header-link {
  font-family: 'Inter', Helvetica, Arial, sans-serif;
  font-size: 0.9rem;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  color: #f5f5f2;
  text-decoration: none;
  opacity: 0.85;
}

.header-link:hover {
  opacity: 1;
}

/* REMOVE PURPLE VISITED LINKS */
.header-link:visited {
  color: #f5f5f2;
}

/* MOBILE */
@media (max-width: 768px) {
  .header-inner {
    padding: 0 18px;
  }

  .header-actions {
    gap: 18px;
  }

  .header-logo a {
    font-size: 2rem;
  }
}
</style>
