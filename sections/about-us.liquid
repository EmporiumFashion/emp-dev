{% comment %}
About Us — Luxury / Streetwear
Image uploaded via admin
Grayscale image with color reveal under marquee
{% endcomment %}

<section class="about">
  <div class="about-inner">

    <!-- IMAGE SIDE -->
    <div class="about-image">
      {% if section.settings.image != blank %}
        <img
          src="{{ section.settings.image | image_url: width: 900 }}"
          alt="About us"
          loading="lazy"
          width="900"
          height="1100"
        >
      {% endif %}

      <!-- diagonal marquee -->
      <div class="about-marquee">
        <div class="marquee-track">
          <span>Lock In.</span>
          <span>Lock In.</span>
          <span>Lock In.</span>
          <span>Lock In.</span>
        </div>
      </div>
    </div>

    <!-- DIVIDER -->
    <div class="about-divider"></div>

    <!-- TEXT SIDE -->
    <div class="about-text">
      <h2>About us</h2>
      <p>
        We started with a simple idea — build something
        <strong>raw</strong>, <strong>timeless</strong>, and
        <strong>intentional</strong>. Emporium was born from nights
        spent obsessing over details, fabric, and culture — not trends.
        This is for those who move with purpose.
      </p>
    </div>

  </div>
</section>

<style>
.about {
  background: #000;
  color: #f5f5f2;
  width: 100vw;
  margin-left: calc(50% - 50vw);
  overflow: hidden;
}

.about-inner {
  display: grid;
  grid-template-columns: 1fr 1px 1fr;
  min-height: 80vh;
}

/* IMAGE */
.about-image {
  position: relative;
  overflow: hidden;
}

.about-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: grayscale(1) contrast(1.05);
  transition: filter 0.3s ease;
}

/* MARQUEE */
.about-marquee {
  position: absolute;
  inset: 0;
  transform: rotate(-12deg);
  pointer-events: none;
  mix-blend-mode: screen;
}

.marquee-track {
  display: flex;
  gap: 80px;
  animation: marquee 12s linear infinite;
  font-family: Inter, Helvetica, Arial, sans-serif;
  font-size: clamp(2.5rem, 6vw, 5rem);
  font-weight: 600;
  color: #fff;
}

/* This makes the image turn COLOR under text */
.about-marquee span {
  mix-blend-mode: difference;
}

/* DIVIDER */
.about-divider {
  background: rgba(245,245,242,0.25);
}

/* TEXT */
.about-text {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  padding: 48px;
  font-family: Inter, Helvetica, Arial, sans-serif;
}

.about-text h2 {
  font-size: clamp(2rem, 4vw, 3rem);
  margin-bottom: 24px;
}

.about-text p {
  max-width: 420px;
  opacity: 0.85;
  line-height: 1.6;
}

/* MARQUEE ANIM */
@keyframes marquee {
  from { transform: translateX(0); }
  to { transform: translateX(-50%); }
}

/* MOBILE */
@media (max-width: 900px) {
  .about-inner {
    grid-template-columns: 1fr;
  }

  .about-divider {
    display: none;
  }

  .about-image {
    min-height: 50vh;
  }
}
</style>

{% schema %}
{
  "name": "About Us",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "About image"
    }
  ],
  "presets": [
    {
      "name": "About Us"
    }
  ]
}
{% endschema %}
