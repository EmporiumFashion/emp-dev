<section class="about-us">
  <div class="about-grid">

    <!-- Image side -->
    <div class="about-image-wrap">
      {% if section.settings.image != blank %}
        <img
          src="{{ section.settings.image | image_url: width: 900 }}"
          alt="About us"
          width="900"
          height="1200"
        >
      {% endif %}
    </div>

    <!-- Divider -->
    <div class="about-divider"></div>

    <!-- Text side -->
    <div class="about-text">
      <h2>About us</h2>
      <p>
        We started with a belief in <strong>discipline</strong>, <strong>focus</strong>,
        and the power of <strong>showing up daily</strong>. What began as an idea
        turned into a movement built on consistency, intent, and obsession with
        craft. This is more than clothing — it’s a mindset.
      </p>
    </div>

  </div>
</section>

<style>
.about-us {
  background: #000;
  color: #fff;
  width: 100vw;
  margin-left: calc(50% - 50vw);
  padding: 0;
}

.about-grid {
  display: grid;
  grid-template-columns: 1fr 1px 1fr;
  min-height: 80vh;
}

/* Divider */
.about-divider {
  background: rgba(255,255,255,0.15);
  width: 1px;
}

/* Image side */
.about-image-wrap {
  display: flex;
  align-items: center;
  justify-content: center;
}

.about-image-wrap img {
  max-width: 70%;
  border: 10px solid #fff;
  transform: rotate(3.5deg);
  object-fit: cover;
}

/* Text side */
.about-text {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 80px;
  font-family: Inter, Helvetica, Arial, sans-serif;
}

.about-text h2 {
  font-size: 3rem;
  margin-bottom: 24px;
  font-weight: 500;
}

.about-text {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: left;
  align-items: center;       /* horizontal center */
  text-align: center;
  padding: 60px;
}
.about-text p {
  max-width: 420px;
  line-height: 1.6;
  opacity: 0.9;
}

.about-text strong {
  font-weight: 600;
}
</style>

{% schema %}
{
  "name": "About us",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "About image"
    }
  ],
  "presets": [
    {
      "name": "About us"
    }
  ]
}
{% endschema %}
